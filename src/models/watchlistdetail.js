export default class WatchlistDetail{
    constructor(id, movieId, title, posterImage){
        this.id = id;
        this.movieId = movieId;
        this.title = title;
        this.posterImage = posterImage;
    }
}